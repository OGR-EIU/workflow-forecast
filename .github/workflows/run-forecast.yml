name: Run forecast

on:
    workflow_call:
    workflow_dispatch:

jobs:
    run-forecast:
        runs-on: ubuntu-latest

        steps:

            - name: Test 1
              run: |
                  pwd
                  ls
                  echo $GITHUB_WORKSPACE

            - name: Clone workflow-forecast repo
              uses: actions/checkout@v3
              with:
                fetch-depth: 1
                path: "./workflow-forecast"

            - name: Set up Python
              uses: actions/setup-python@v4
              with:
                python-version: "3.11"

            - name: Install Python dependencies
              run: | 
                cd $GITHUB_WORKSPACE
                pip install -r ./workflow-forecast/requirements.txt

            - name: Prepare environment
              run: |
                cd $GITHUB_WORKSPACE
                echo ===========================================
                cd $GITHUB_WORKSPACE/workflow-forecast
                git status
                git branch
                echo ===========================================
                cd $GITHUB_WORKSPACE
                cp ./workflow-forecast/artifact/*.* ./
                pwd
                ls
                # python prepare_forecast_environment.py --local=no
                git clone https://${{ secrets.token_with_all_access}@github.com/OGR-EIU/model-ea model
                echo ===========================================
                ls
                echo ===========================================
                cd $GITHUB_WORKSPACE

